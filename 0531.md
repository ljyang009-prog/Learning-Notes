# 文件消息的摘要 base64 编码

## openssl

安装库和文档
sudo apt install libssl-dev libssl-doc
路径
cd usr/include/openssl

## 哈希、摘要算法 MD5,SHA1,SHA256

- 任意长度的输入，生成固定长度的输出
- 唯一性（可以做签名、加密）
- 单向,不可逆

## base64 编码、解码

- 把二级制（文本）编码为可打印的文本格式，变大为 4/3
- 可逆，可以解码

### 构成

- 字符 26*2 = 52
- 数字 0 - 9  10
- 2个符号 /+ =
任意格式转换成可打印的文本、电子邮件

## 任务

1. 切分文件，bit torrent 协议，比特湍流 p2p
    512k
    ./splice xxx.pdf

meta.info 存储每一片的 sha128 值，最后一行整体文件的
