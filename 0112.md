# 动态内存管理

- malloc
- calloc
- realloc

## void *

通用指针

- 存储其他类型的指针的值
- 不能解引用
- 强制转换为具体指针再解引用

void * 不是空指针

空指针: NULL 宏定义，0
野指针：没有初始化，被free后的，不确定行为

```c
    char c;
    int i;

    //具体指针
    char *p1 =&c;
    cahr *p2 = &i;

    void *p;
    p = p1; //char *
    p = p2; //int *
    
    int *p3 = (int *)p;
    *p3 = 2;


```

## 流程

1.使用malloc 分配内存
2.若空间不足realloc重新分配,指针的值可能发生改变
    ptr = realloc(ptr, size);
3.必须使用 free 释放内存,可以赋空NULL
    free(ptr);
    ptr = NULL;
4.calloc()等价于malloc()后调用memset(ptr,0,size);

## 作业

1.改写转16进制函数，函数返回字符指针
2.实现字符串乘、除
3.使用1-9的数字，构造三个三位数a,b,c
    b=a*2
    c=a*3
    数字不能重复
    123 246 369（x）
