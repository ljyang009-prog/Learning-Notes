# 技术面
## TCP 与 UDP 的区别
- tcp: 连接 可靠传输 字节流 一对一
- udp: 无连接 不可靠 数据报 广播
## 红黑树与二叉树的区别
- 红黑树：特殊的平衡二叉搜索树 查找的时间复杂度为O(logn)
1. 有红黑两种颜色
2. 根节点和叶子节点必须为黑
3. 红色节点的子节点必须为黑
4. 从任一节点到其叶子节点所经过的黑色节点的数量一致
- 二叉树： 左子树所有节点小于根节点，右子树所有节点大于根节点。
## 哈希表与哈希冲突
- 哈希表：一种通过键（Key）直接访问内存存储位置的数据结构，它实现了平均 O(1) 的时间复杂度
- 哈希冲突：不同的 Key 经过哈希函数计算并映射到数组后，得到了相同的数组下标
- 解决方法：
开放定址法：线性探测和平方探测
拉链法
## http的长连接和短链接
短连接（HTTP/1.0 默认）：

机制： 客户端每发起一次 HTTP 请求，都需要先进行 TCP 三次握手建立连接；服务器响应后，立即通过四次挥手断开 TCP 连接。

缺点： 每次请求都有 TCP 建立和断开的开销，导致页面加载慢，服务器压力大。

长连接（HTTP/1.1 默认，Keep-Alive）：

机制： 客户端与服务器建立一次 TCP 连接后，可以保持连接不断开，后续的多个 HTTP 请求都复用这个 TCP 连接。直到客户端或服务器主动关闭，或者超时。

优点： 减少了 TCP 握手和挥手的开销，提高了传输效率，降低了服务器负载。

实现： 在 HTTP 请求头中设置 Connection: keep-alive。

长连接的维护与优化：

超时时间： 服务器通常会设置一个 Keep-Alive Timeout，如果连接空闲超过这个时间，服务器会主动关闭连接。

管道化（Pipelining）： HTTP/1.1 支持在同一个连接上连续发送多个请求，但响应必须按顺序返回（存在队头阻塞问题）。

多路复用（Multiplexing）： HTTP/2.0 引入了多路复用，在同一个 TCP 连接上可以并行交错地发送多个请求和响应，彻底解决了 HTTP/1.1 的队头阻塞问题。

适用场景：

短连接： 适用于并发量大、请求频率低、数据量小的场景（如早期的静态网页）。

长连接： 适用于频繁请求、实时性要求高的场景（如WebSocket、即时通讯、数据库连接池、微服务间的 RPC 调用）。