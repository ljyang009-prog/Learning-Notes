# 字符串

## 定义

字符串：字符的序列

实现

- 字符数组
- 字符指针

```c
    //字符数组
    char s1[] = {'H', 'e', 'l', 'l', 'o'};
    //字符数组，字符串
    char s2[] = {'H', 'e', 'l', 'l', 'o', '\0'};

    char *s3 = "Hello";
    char s4[] = "Hello";

```

## 区分 字符数组与字符指针

```c
    //数组是一个指针常量
    char s1[] = "Hello";
    s1[0] = 'B';
    printf("s1=%s\n", s1);

    //指针变量，可以指向其他地址空间
    //字符串常量，不可变
    char *s2 = "Hello";
    s2 = "bee";
    printf("s2=%s\n", s2);

```
