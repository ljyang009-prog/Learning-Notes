# 数据库

## SQL 优化
- 索引：B+Tree 3~4 叶子节点 双向链表（范围查找），Hash
    - 自动：主键、外键、唯一约束
    - 手动：where、join、order by、group by
    - 避免了全表扫描、回表问题
    - 影响插入、更新、删除的性能
- 优化 SQL 语句
    - 禁用 SELECT *
    - 避免子查询和临时表（基于执行计划再次优化）
    - join 小表（）驱动大表
    - 批量写入：
        - 2000 insert into t1 values(), (), (), (), ();
        - 并入事务
    - 执行计划 explain
        - `explain SELECT ...`
- 缓存（*）
    - redis
- 分表
    - 水平拆分（行）
        - 表分区：数值范围 range、list、hash
        - 水平分表：create table t1_p1 as select ...;
    - 垂直拆分（列）
- 分库
    - 读写分离：主从架构，从主库写，从库读

```sql
CREATE TABLE t1 (
    id INT,
    year_col INT
)
PARTITION BY RANGE (year_col) (
    PARTITION p0 VALUES LESS THAN (1991),
    PARTITION p1 VALUES LESS THAN (1995),
    PARTITION p2 VALUES LESS THAN (1999),
    PARTITION p3 VALUES LESS THAN (2003),
    PARTITION p4 VALUES LESS THAN (2007)
);

begin;
insert into t1 values(1, 1900);
insert into t1 values(2, 2000);
insert into t1 values(3, 1997);
insert into t1 values(4, 1980);
insert into t1 values(5, 1999);
insert into t1 values(6, 1979);
insert into t1 values(7, 2001);
insert into t1 values(8, 2005);
insert into t1 values(9, 1990);
insert into t1 values(10, 1998);
commit;


select * from t1 PARTITION (p1);

-- 备份了一个表
CREATE TABLE t1_p2
as
select * from t1 partition (p2);

delete from t1 partition (p2);

```

## Redis

https://redis.com.cn/topics/why-use-redis.html

功能
- 缓存服务 KeyValue Hash
- 数据结构实现

数据类型
- string：普通缓存、计数器
- list：队列、栈
- set：无重复值
- sorted set：排序
- hash
- ...

### 安装

    sudo apt install redis

    sudo service redis status

    sudo netstat -tpnl | grep redis

### 使用
- redis-cli

### API 手册
http://doc.redisfans.com/


## HTTP 压力测试

测试服务器程序的吞吐量，每秒接收请求/响应的次数、读写性能

    nginx
        多进程 + epoll
        线程池 + epoll
    redis

- ab
- wrk

    wrk -t8 -c200 -d30s http://127.0.0.1


## 进程

进程：程序的一次执行过程，及持有的资源的总和；每个进程独立的内存空间
线程：进程中独立的执行单元，多个线程有共享内存资源

### 多进程
- fork 创建一个子进程
- exec
- getpid    进程ID
- getppid   父进程ID

### 进程类型
- 孤儿进程：父死子活，子进程的 PPID=1, /systemd
- 僵尸进程：父活子死，子进程资源未回收，子进程状态 Z
- 守护进程：

    ps -ef | grep abc

```c
// p1.c
#include <stdio.h>
#include <stdlib.h>

// unix std - POSIX 规范定义的标准 API
#include <unistd.h>
#include <sys/wait.h>

int main(int argc, char const *argv[])
{
    int pid = fork();
    if (pid == -1)
    {
        perror("创建失败\n");
        return EXIT_FAILURE;
    }

    if (pid == 0)
    {
        // 子进程的空间
        printf("worker: %d\t%d\n", getpid(), getppid());
        sleep(5);

        // 返回状态码
        exit(42);
    }
    else
    {
        printf("master: %d\t%d\n", getpid(), getppid());

        // 获得子进程返回的状态码
        int status;
        waitpid(pid, &status, WUNTRACED);
        if (WIFEXITED(status))
        {
            int val = WEXITSTATUS(status);
            printf("val=%d\n", val);
        }

        // 主进程
        sleep(10);
        printf("pid: %d\n", pid);
    }
    
    return 0;
}

```


```c
// p2.c
// 多进程示例
#include <stdio.h>
#include <stdlib.h>

#include <unistd.h>
#include <sys/wait.h>

#define N 8

int main(int argc, char const *argv[])
{
    for (int i = 0; i < N; i++)
    {
        int pid = fork();
        if (pid == 0)
        {
            printf("worker: %d, %d\n", getpid(), getppid());
            sleep(20);
            exit(0);
        }
    }
    
    for (int i = 0; i < N; i++)
    {
        wait(NULL);
    }
    printf("master: end\n");
    
    return 0;
}

```

### exec - 子进程执行独立任务

- execl: list
- execv: array

```c
// p1.c
#include <stdio.h>
#include <stdlib.h>

// unix std - POSIX 规范定义的标准 API
#include <unistd.h>
#include <sys/wait.h>

int main(int argc, char const *argv[])
{
    int pid = fork();
    if (pid == -1)
    {
        perror("创建失败\n");
        return EXIT_FAILURE;
    }

    if (pid == 0)
    {
        // 子进程的空间
        printf("worker: %d\t%d\n", getpid(), getppid());
        // sleep(5);

        // exec - execute
        // execl list：多个参数，逗号分隔
        // execv array：一个数组
        // execlp("wc", "wc", "-l", "p2.c", NULL);

        // 1. 路径
        // 2. 程序名
        // 3. 参数
        //  . NULL 标识参数结束
        // execlp("uname", "uname" "-a", NULL);

        execlp("./m1", "m1", "Hello", NULL);

        // 返回状态码
        exit(42);
    }
    else
    {
        printf("master: %d\t%d\n", getpid(), getppid());

        // 获得子进程返回的状态码
        int status;
        waitpid(pid, &status, WUNTRACED);
        if (WIFEXITED(status))
        {
            int val = WEXITSTATUS(status);
            printf("val=%d\n", val);
        }

        // 主进程
        // sleep(10);
        printf("pid: %d\n", pid);
    }
    
    return 0;
}


// m1.
#include <stdio.h>

int main(int argc, char const *argv[])
{
    if (argc < 2)
    {
        printf("参数不足\n");
        return 1;
    }
    
    printf("这是另一个程序：%s\n", argv[1]);
    return 0;
}


```

## 任务：ch37 创建守护进程

    sshd, mysqld, httpd, xxxd