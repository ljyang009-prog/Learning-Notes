# C++ - 面向对象编程


## 对象与类
类：对象的定义（模板），对具有相同属性、行为的一组对象的定义
对象：具体存在，类的实例

## 组合：整体与部分的关系
- 组合:强组合，部分不能脱离整体存在，整体销毁部分销毁
- 聚合：弱组合，部分可以独立存在
- 关联：存在联系，不是整体与部分

```c++

// 属性对象：强组合，生命周期捆绑，一致
    class Line
    {
        Point s;
        Point e;
    }

// 指针：弱组合、聚合，生命周期独立
    class Line
    {
        Point *s;
        Point *e;
    }
// 引用：关联关系
    class Line
    {
        Point &s;
        Point &e;
    }

    
```

## 封装

## 继承

角色
- 父类、基类、超类
- 子类、派生类：获得父类中的属性和行为

特性
- 代码的重用、多个类公共的成员提取出来放入父类中，封装
- 易于维护和扩展：修改父类，子类也同步

类型
- 单继承
- 多继承：存在负作用

属性
- 子类拥有父类中所有属性：私有（不能直接访问，可通过构造函数初始化）、受保护、共有
- 子类中可以访问父类的受保护、共有的
- 受保护的属性子类可见

方法
- 子类可以覆盖父类的方法，重写 override，父类的方法是虚函数
- 指针类型与对象类型不一致，父类的析构函数是虚的，否则子类的析构执行

## 多态

方法行为的多态，根据对象类型，参数类型、参数个数选择不同版本的方法执行，呈现不同的行为
- 重载：一个类内部，同名方法有多个版本，参数类型与参数个数不同，编译时选择不同方法执行
- 重写：在继承关系中，子类覆盖了父类的同名方法，根据对象的类型选择

## 纯虚函数与抽象类



## 任务

1. 定义一下类
    - 品牌
        - id,name,price,vector<产品>
    - 商品
        - id,name,price,品牌
    - 客户
    - 地址
    - 订单
    - 订单项

2. 燃油、电力（新能源）
    车：油车，电车，混合动力
    摩托：有车，电车


