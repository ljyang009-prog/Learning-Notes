# 指针、数组、字符串

## 指针

### 野指针

- 声明，未赋值的指针叫野指针
- 野指针不能解引用

避免野指针

- 在声明指针时初始化有效值
- 声明时赋值NULL（0）
- 不要随意修改指针的值

### 空指针

- 初始化时，赋值NULL（0）
- 动态内存管理，free之后，赋值NULL

### 空指针是确定值，野指针是不确定值

## 指针与数组

数组名
-数组的地址
-数组第一个元素的地址

### 指针的运算（地址的偏移，单位为数据类型的大小）

- 指针加法
- 指针减法
- 俩个指针相减

```c
int a[] = {1, 2, 3, 4, 5};
int *p = a; // p指向数组a的首元素

a++;//错误
p++;//有效

```

## 常量指针与指针常量

const

```c
//常量指针：值不可变
int const *p1; // p1是指向常量的指针，不能通过p1修改所指向的值

//指针常量：指针不可变
int *const p2; // p2是常量指针，不能修改p2的指向

//指向常量的指针常量：都不可变
int const *const p3;

```

## 指针与函数

函数返回指针

- 指针变量是 static 类型
- 指针的数据动态分配

```c
    int max(int *a, int *b) 
    {
        return (*a > *b) ? *a : *b;
    }

    int *f()
    {
        static int *a;
        return a;
    }

    
    int *f()
    {
        int *p = malloc(sizeof(int));//手动分配
        return p;
        //free(p);
    }
    在函数外可以继续访问空间
```
