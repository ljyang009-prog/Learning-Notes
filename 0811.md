# C++ 面向对象编程

## 字符串与vector

### 字符串
- C char str[],char *
- C++ std::string

- 想等
- 拼接
- 

```c
    for (auto r : s1)           // r 局部变量，值拷贝：只读
    for (ato &r : s1)           // 引用，可以修改
    for (const auto &r : s1)    // 常量引用，不可以修改，只读，性能优
    for (auto &&r : s1)         // 万能引用，可以修改，临时变量

    // 容器，模板编程
    vector<int> v1;        // 0
    vector<int> v2(10);    // 10-0
    vector<int> v3(10, 1); // 10-1
    vector<int> v4{10, 1}; // 10,1
```

## 面向对象编程 - OOP

- OOA 分析
- OOD 设计
- 00P 编程

一种程序设计方法学，指导进行实践的理论或原则，编程范式

编程范式
- 面向过程:C、pascal
- 函数式编程:Lisp
- 面向对象：C++、Python

面向对象编程
- 对象：实体和概念在程序中的投影，分离出关注点，一个模型描述

OOP 特性
- 封装
- 继承
- 多态
UML 统一建模语言
- 一组OOA/OOD 一组图形化工具
- 建模、设计、构图

### 类与对象

类：对具有属性、行为的一组对象下的定义；模板
对象：具体的存在，类的实例

对象：
## IM
- 协议
- 数据存储，推荐文档：MongoDB
- 会话

## 作业
1. 定义三个类 point.h  point.cxx   main.cxx
    - Point
        - int x
        - int y
        - Point(int x)
        - ~Point(int x, inty)
        - void show()