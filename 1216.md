# 位运算 & 字符

## 位运算

 数值转换为二进制逐位进行运算，结果数值

- `&` 按位与：两个位都为1，结果才为1，否则为0
- `|` 按位或：两个位有一个为1，结果就为1，否则为0
- `^` 按位异或：两个位不相同，结果为1
- `~` 按位取反：0变1，1变0
- `<<` 左移：将一个数的二进制位左移若干位，右边补0
- `>>` 右移：将一个数的二进制位右移若干位，左边补0或1，取决于最高位是0还是1

### &

两个1得1，其余得0，掩码运算

### |

两个0得0，其余得1，掩码运算

### ^

两个1得0，其余得1，掩码运算

### ~

0变1，1变0，掩码运算

### <<

左移，乘以2的n次方

### >>

右移，除以2的n次方
