# 数据库

## 数据库

数据库管理系统(DBMS):（数据库服务器）：是一个软件,核心功能创建数据库，操作数据库，数据库的备份与恢复

`数据库`：磁盘上的一组文件（数据文件、日志文件、控制文件等）

`数据库实例`：数据库服务器加载数据库文件创建的进程

## 数据库类型
- 关系数据库
    - MySQL     * C++ 开源
    - Oracle    ++商业受限
    - Db2       ++商业受限
    - PostgreSQL  * C 开源
    - SQLite    嵌入式
    - ...
- 对象数据库
- NoSQL 不使用 SQL 语言的数据 Not Only SQL
    - 文档数据库：mongoDB
    - 键值数据库：resis(hashtable)
    - 行数据库：  hbase,clickHourse
    - 图形数据库：neo4J
    - 时序数据库：

Word,word文档
## MySQL

安装:
    sudo apt install mysql-server
状态：systemctl status mysql
端口：
    sudo netstat - tpnl | grep mysql
启动/关闭：
    sudo service mysql start
    sudo service mysql stop

### 链接
- mysql 客户端
- mysql GUI 工作台
- navcat 第三方
- ...

    sudo mysql

### MySQL 核心命令

show databases;

create database abc;

删除数据库
drop database abc;

连接
use abc;
\s
显示表
show tables;

查看表中的数据
select *from user;
select *from user\G

指定列的名称
select host,user,from user;

### 结构化查询语言

一套技术规范，标准化 SQL 

### DDL 数据定义语言
- `CREATE`      创建
- `DROP`        删除
- `ALTER`       修改
- `TRUNCATE`    截断
- ...
### DML 数据操作语言 CRUD CreateReadUpdateDelete crud boy
- `INSERT`      增
- `DELETE`      删
- `UPDATE`      改
- `SELECT`      查
- ``
### TCL 事物控制语言
- `BEGIN`       开始
- `COMMIT`      提交
- `ROLLBACK`    回滚

```sql


create table person
(
    name varchar(16) NOT NULL,
    age int default 18,
    gender enum('male','female') default 'male',
    tel char(11)
);

insert person values('bob',21,'male','18658748456');
insert person(name,age) values("jack",21),("lisa",28);

select * from person;

```

## 数据类型
- 数值
    - bit           1
    - tinyint       2
    - smallint      4   number
    - int/integer   8
    - bight
    - flaot
    - double
    - decimal(7,2)
- 日期与时间
    - time
    - datetime
    - timestamp
    - year
- 字符串
    - char 255  长度固定 快
    - varchar 65535  长度可变， 空间 N+1,
    - text/tinytext 文本数据
    - binary/blob 二进制数据，不存图片和多媒体
    - enum
    - set
- 空间数据类 GIS
    - 点
    - 线
    - 面
- JSON
```js
    {
        "x":2,
        "y":"hello",
        "z":true,
        "n":[1,2,3,4]
        "m":
        {
            a:1,
            b:"hi"
        }
    }

```

```sql
create table t1
(
    c1 date,
    c2 time,
    c3 datetime,
    c4 timestamp,
    c5 year
);

insert t1 values(now(),now(),now(),now(),now());
insert t1 values("2008-8-3","14:39:56",sysdate(),NULL,NULL);
函数：
- sysdate()
- systime()
- now()

select adddate(curdate(),30);
select datediff(now(),"2005-7-24");

```
![](/images/time.png)

```sql
create table t2
(
    c1 enum("男","女","保密"),
    c2 set("A","B","C","D")
);

insert t2 values("男","A,B,C");
insert t2 values(1,3),(2,7),(3,8);
1: A
2: B
3: A B
4: C
7: A B C
8: D
```
![](/images/string.png)

> 文档数据库 MongoDB, JSON/BSON, 爬虫，聊天记录，文件

create table t3
(
    c1 JSON
);
insert t3 values('{x:1}')

## 关系建模
表存储
- 实体或概念特征或则属性，表由多个列（字段），由多行（记录）构成
- 实体与实体之间的关系

关系建模 - 关系代数

关系范式
- 1NF:列的数据原子性
- 2NF：所有字段直接依赖于主键字段，创建复合主键
- 3NF：

```sql
// PK 主键约束
// FK 外键：外键字段是另一张表（另一个记录）的主键值
    create table t5
    {
        id          int auto_increment primary key
        name        varchar(16),
        gender      enum('M','F'),
        birthday    date,
        tel         char(11),
        mother      int 

    };

```

## 任务
1.关系建模
    品牌，产品，客户，订单项，地址
    