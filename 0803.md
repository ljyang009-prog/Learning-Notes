# 数据库

## 数据库

数据库管理系统(DBMS):（数据库服务器）：是一个软件,核心功能创建数据库，操作数据库，数据库的备份与恢复

`数据库`：磁盘上的一组文件（数据文件、日志文件、控制文件等）

`数据库实例`：数据库服务器加载数据库文件创建的进程

Word,word文档
## MySQL

安装:
    sudo apt install mysql-server
状态：systemctl status mysql
端口：
    sudo netstat - tpnl | grep mysql
启动/关闭：
    sudo service mysql start
    sudo service mysql stop

### 链接
- mysql 客户端
- mysql GUI 工作台
- navcat 第三方
- ...

    sudo mysql

### 核心操作

show databases;

create database abc;

删除数据库
drop database abc;

连接
use abc;
显示
show tables;
