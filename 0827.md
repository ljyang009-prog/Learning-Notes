# OpenCV

## 图形、图像

图形：工具绘制
图像：设备采集并进行数字编码存储到计算机

- 位图：BMP 存储了各个（每个）像素的原始信息
- 有损压缩
    - PNG：32位深，4通道（alpha通道 透明度），便于网络传输
    - JPEG：24，3通道，照片
    - gif：8，彩色，多帧合并
    - webP：压缩比例极低
    - ...

## 视频采集

// 播放器
sudo apt install vlc
// 解码器
sudo apt install ffmpeg

```c++
// vd.cxx
#include <iostream>
#include "opencv2/opencv.hpp"

using namespace std;
using namespace cv;

int main(int argc, char const *argv[])
{
    // 视频采集
    VideoCapture capture;

    if (!capture.open(0, CAP_ANY))
    {
        cout << "open error" << endl;
        return -1;
    }
    // 图像容器，存储一画面
    Mat frame, bin;

    while (1)
    {
        // 解码、获得下一帧
        capture.read(frame);
        // 色彩空间转化：转灰度图
        cvtColor(frame, bin, COLOR_BGR2GRAY);
        // 二值化
        // threshold(bin, bin, 127, 255, THRESH_BINARY);
        // 边缘检测
        Canny(bin, bin, 128, 255);

        putText(frame, "2026/08/27", Point(0, 20), FONT_HERSHEY_PLAIN, 1, Scalar(255,255,255));
        // putText(bin, "2026/08/27", Point(0, 20), FONT_HERSHEY_PLAIN, 1, Scalar(255,255,255));
        // show
        imshow("video", frame);
        imshow("bin", bin);

        if (waitKey(100) == 'q') break;
    }    
    capture.release();
    destroyAllWindows();

    return 0;
}

```

## 视频录制

sudo apt install ffmpeg

查看视频文件信息
ffmpeg -i out.mp4

视频格式
- mp4
- avi
- mov
- mtk
- ogg
- webM
- ...

编码器
- m,p,4,v   .mp4        常见
- a,v,c,1   .mp4 h.264  小，工业标准，推荐
- h,e,v,1   .mp4 h.265  4K, 占CPU，最新
- x,v,i,d   .avi        

ffmpeg -i xxx.mp4


```c++
// vd2.cxx
// 视频录制：采集、存储
#include <iostream>
#include "opencv2/opencv.hpp"

using namespace std;
using namespace cv;

int main(int argc, char const *argv[])
{
    VideoCapture capture;
    VideoWriter out;

    if (!capture.open(0, CAP_ANY))
    {
        cout << "error" << endl;
        return -1;
    }

    // 1. 存储路径
    // string path = "out.mp4";
    string path = "h264.mp4";
    // string path = "h265.mp4";
    // string path = "out.avi";
    // 2. 四字符的编码器代号，与 FFmpeg 库相关
    // int fourcc = VideoWriter::fourcc('m', 'p', '4', 'v');
    int fourcc = VideoWriter::fourcc('a', 'v', 'c', '1');
    // int fourcc = VideoWriter::fourcc('h', 'e', 'v', '1');
    // int fourcc = VideoWriter::fourcc('X', 'V', 'I', 'D');
    // 3. 每秒帧数：10、24, 60
    int fps = 10;
    // 4. 画面分辨率
    int w = capture.get(CAP_PROP_FRAME_WIDTH);
    int h = capture.get(CAP_PROP_FRAME_HEIGHT);
    Size size(w, h);
    // 5. 是否彩色画面

    if (!out.open(path, fourcc, fps, size, false))
    {
        cout << "写 初始化失败" << endl;
        return -1;
    }

    Mat frame, gray;
    for (int i = 0; i < 600; i++)
    {
        // 采集
        capture.read(frame);
        // 写入
        // resize(frame, frame, size);

        cvtColor(frame, gray, COLOR_BGR2GRAY);
        out.write(gray);
        // 显示
        imshow("v", gray);
        if (waitKey(100) == 'q') break;
    }
    capture.release();
    // 释放资源
    out.release();

    return 0;
}

```

## 视频处理