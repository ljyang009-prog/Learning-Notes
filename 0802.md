# 网络编程

## HTTP 服务

HTTP 超文本传输协议, HTML 超文本标记语言（a.html）
- HTTP 1.0      TCP
- HTTP 2.0  `*` TCP
- HTTP 3.0      UDP `?`

1990 HTTP, HTML(Web)

超文本
- 超链接：graph 网
- 图片
- 音频
- 视频

PageRank：
- 超链接，投一票
- 每个页面的权重不同

### HTTP 服务器
- apache
- nginx

### nginx

sudo apt install nginx

ps ef | grep nginx

service nginx statut

service nginx stop/start

sudo netstat -tpnl | grep nginx

http://127.0.0.1:80

获得实际 IP
ip a
http://IP:80


### HTTP 协议

阅读：图解 HTTP

nginx 去目录 /var/www/html 查找文件

sudo chmod 777 abc

请求中包含文件的后缀名，
响应时，根据后缀名确定文件类型 application/pdf, audio/mp3
`MIME`

```c
#include <unistd.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <sys/stat.h>
#include <netinet/in.h>

#define PORT 9000

int main(int argc, char const *argv[])
{
    int fd = socket(AF_INET, SOCK_STREAM, 0);

    int opt = 1;
    setsockopt(fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));

    struct sockaddr_in addr;
    memset(&addr, 0, sizeof(addr));
    addr.sin_family = AF_INET;
    addr.sin_port = htons(PORT);
    addr.sin_addr.s_addr = htonl(INADDR_ANY);

    bind(fd, (struct sockaddr *)&addr, sizeof(addr));
    listen(fd, 1024);

    while (1)
    {
        struct sockaddr_in c_addr;
        memset(&c_addr, 0, sizeof(c_addr));
        socklen_t len;

        int c_fd = accept(fd, (struct sockaddr *)&c_addr, &len);

        char buf[1024 * 4];
        int n = read(c_fd, buf, sizeof(buf));
        printf("%s\n", buf);

        char header[] = "HTTP 1.1 200 OK\n"
                        "Content-Type: text/html\n"
                        "Content-Length: 32\n"
                        "Server: myHTTP\n"
                        "\n"
                        "<h1>Hello HTTP</h1>";

        write(c_fd, header, sizeof(header));

        close(c_fd);
    }

    close(fd);

    return 0;
}

```