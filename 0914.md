# 邦普技术面
## 视觉实时追踪系统
### 视觉模块与单片机怎么通信
- MaixCam和STM32使用 UART5，配置为 115200、8位数据、1位停止位、无校验。
- STM32通过 HAL 的单字节中断接收，每接收一个字节就进入串口回调函数。
- 再通过状态机识别 X/Y/Z/E 字段，完成一帧数据解析。
### 步进电机驱动
- 步进电机采用 STEP/DIR 控制方式。STM32的 TIM1 和 TIM3 输出 PWM 脉冲作为 STEP信号，GPIO输出 DIR信号控制方向。
- 步进频率由定时器自动重装值决定，频率越高，电机速度越快。定时器更新中断中会逐步调整脉冲周期，实现加速、匀速和减速，避免电机突然启动导致失步。
- TMC2209则通过 USART1 配置细分、电流和斩波参数。
- 为了保证系统安全，如果超过一定时间没有收到新的有效视觉帧，程序会认为目标丢失，并停止两轴电机。同时还通过 USART2提供在线参数调节和遥测功能，可以修改PID参数、速度上限和加速度参数。
### 控制算法
- 控制算法采用 PID。目标位置设置为画面中心，也就是水平和垂直偏差都为0。
- PID的输入是视觉偏差，输出是步进电机的目标速度。
- 程序还根据误差大小对速度上限和加速度进行分段调节：误差大时提高速度和加速度，接近目标时降低速度，减少过冲和振荡。

## 环境检测与上云
### dht11 单总线
通信过程如下：
1. 主机先将数据线配置为输出，拉低至少 18 ms，通知 DHT11 开始通信。
2. 主机释放总线并等待约 20~40 μs。
3. DHT11 返回响应信号：
   - 先拉低约 80 μs；
   - 再拉高约 80 μs。
4. 随后 DHT11 发送 40 位数据：
   - 湿度整数 8 位；
   - 湿度小数 8 位；
   - 温度整数 8 位；
   - 温度小数 8 位；
   - 校验和 8 位。
每一位数据都以约 50 μs 的低电平开始，之后根据高电平持续时间判断：
- 高电平约 26~28 μs，表示 0；
- 高电平约 70 μs，表示 1。
最后校验：
校验和 = 湿度整数 + 湿度小数 + 温度整数 + 温度小数
取低 8 位与接收到的校验值比较。
在 STM32 中实现时，重点是：
- 精确控制微秒级延时；
- 及时切换 GPIO 输入输出模式；
- 设置超时，防止总线异常时程序死循环；
- 读取频率不能太高，DHT11 通常建议间隔至少 1 秒；
- 读取过程中最好关闭可能影响时序的中断，或者使用定时器辅助测量脉宽。

### ADC 采集
ADC 的作用是把模拟电压转换成数字量。以 STM32F103 的 12 位 ADC 为例，转换结果范围是：
0 ~ 4095
如果参考电压为 3.3 V，则输入电压可以计算为：
Vin = ADC_Value × 3.3 / 4095
ADC 采集的一般流程是：
1. 开启 GPIO 和 ADC 外设时钟；
2. 将对应 GPIO 配置为模拟输入；
3. 配置 ADC 分辨率、扫描模式、连续转换模式和采样时间；
4. 选择规则通道和通道序列；
5. 启动 ADC 校准；
6. 启动转换；
7. 查询标志位或使用 DMA 获取转换结果；
8. 对数据进行滤波和换算。
STM32F103 中常见的采集方式有：
- 单次转换：适合偶尔读取；
- 连续转换：适合持续采样；
- 定时器触发：保证固定采样周期；
- DMA 传输：减少 CPU 参与，适合多通道或高速采样。
为了提高采集稳定性，通常会做：
- 多次采样取平均值；
- 中值滤波，去除突发干扰；
- 滑动平均或低通滤波；
- 合理设置 ADC 采样时间；
- 模拟地和数字地合理布线；
- 输入信号增加 RC 滤波；
- 进行 ADC 校准和零点修正。
如果采集的是传感器信号，还需要根据传感器输出范围进行分压、放大或电平匹配，确保输入电压不超过 ADC 参考电压。
### ESP8266 如何使用
ESP8266 通常作为 Wi-Fi 通信模块使用，常见有两种方式：
第一种：使用 AT 指令
STM32 通过 USART 与 ESP8266 通信，ESP8266 固件运行 AT 指令。
基本流程是：
1. STM32 发送 AT，确认模块正常；
2. 设置 Wi-Fi 模式，例如 Station 模式；
3. 连接路由器；
4. 查询 IP 地址；
5. 建立 TCP 或 UDP 连接；
6. 发送传感器数据；
7. 接收服务器响应；
8. 必要时断开连接或维持长连接。
常见指令包括：
AT
AT+CWMODE=1
AT+CWJAP="SSID","PASSWORD"
AT+CIFSR
AT+CIPSTART="TCP","服务器IP",端口
AT+CIPSEND
STM32 端一般需要设计：
- 串口接收中断或 DMA；
- 环形缓冲区；
- AT 指令状态机；
- 超时机制；
- 重试和断线重连；
- 接收数据的帧解析。
第二种：ESP8266 独立运行程序
也可以使用 Arduino、ESP-IDF 或 SDK 直接给 ESP8266 编程，让它自己完成：
- Wi-Fi 连接；
- 传感器采集；
- MQTT、HTTP 或 TCP 通信；
- 云平台数据上传。
如果系统中已经有 STM32，常见架构是：
传感器 → STM32 → UART → ESP8266 → Wi-Fi → 服务器或云平台
STM32 负责实时采集和控制，ESP8266 负责网络通信，这样可以降低系统耦合。
使用 ESP8266 时需要注意：
- 供电电压为 3.3 V；
- Wi-Fi 发射时瞬时电流较大，电源通常要保证 300~500 mA 以上的稳定能力；
- STM32 与 ESP8266 的串口电平要匹配；
- ESP8266 的 EN/CH_PD 引脚需要拉高；
- 复位脚和启动配置脚要正确处理；
- 发送数据要考虑网络延迟、丢包和断线；
- 实际项目中建议采用 MQTT 或 HTTP，而不是自己设计复杂的应用层协议。