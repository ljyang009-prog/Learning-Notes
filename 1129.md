# 运算

- `算数运算`
- `赋值`
- `比较`
- `逻辑运算`
- `位运算`
- `三元运算`

## 算数运算

- 加法 `+`
- 减法 `-`
- 乘法 `*`
- 除法 `/`
- 求余 `%`
- 自增 `++`
- 自减 `--`

## 赋值

- 赋值 `=`
- 加赋值 `+=`
- 减赋值 `-=`
- 乘赋值 `*=`
- 除赋值 `/=`
- 求余赋值 `%=`

- `比较运算符`
- `逻辑运算符`
- `位运算符`
- `三元运算符`

## 比较运算符

- `==` 等于
- `!=` 不等于
- `>` 大于
- `<` 小于
- `>=` 大于等于
- `<=` 小于等于

## 流程控制

- `顺序结构`
- `分支结构`
- `循环结构`

### 分支语句

- `if`
- `if-else`
- `switch() {case}`

## 逻辑运算符

- `&&` 与
- `||` 或
- `!` 非

## 位运算符

- `&` 与
- `|` 或
- `^` 异或
