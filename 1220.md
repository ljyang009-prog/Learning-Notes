# 字符、字符串、二维数组

## 字符

char 字符类型
字符： 表示客观事物或抽象概念的符号
举例： a、b、c、d、1、2、3、$、#

ANSICC      1字节  256
GBK/GB2312 2字节  65535
big5
unicode    1~4
拉丁字符   单字节       char
CJK     多字节      char

## 字符串

```c
    //字符串：字符构成的序列
    char s1[] = "abc";
    char s2[] = {'a','b','c','\0'};
    char s3[] ={'a','b','c'}; //不等价



## 二维数组

杨辉三角
```c
    int arr[5][5] = {0};
    for(int i=0;i<5;i++){
        for(int j=0;j<=i;j++){
            if(j==0 || j==i){
                arr[i][j] = 1;
            }else{
                arr[i][j] = arr[i-1][j-1] + arr[i-1][j];
            }
        }
    }
    //打印杨辉三角
    for(int i=0;i<5;i++){
        for(int j=0;j<=i;j++){
            printf("%d ",arr[i][j]);
        }
        printf("\n");
    }
```

## 作业

1. 了解字符集与字符编码的关系
2. 了解 ANSICC、GBK、utf-8 、unicode
