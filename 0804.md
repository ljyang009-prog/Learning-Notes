# 数据库 - SQL

## DDL 数据定义语言
- CREATE
- ALTER
- DROP
- TRUNCATE
- GRANT

```sql
    create database udb;
    use udb;
    show tables;

    create table t1
    (
        id int auto_increment primary key,
        c2 char(4),
        c3 decimal(7,2)
    );

    ALTER TABLE t1 ADD c4 varchar(16) DEFAULT 'N';
    ALTER TABLE t1 CHANGE c2 title varchar(4);
    ALTER TABLE t1 DROP COLUMN c3;
    ALTER TABLE `t1` CHANGE `c4` `order` VARCHAR(16);

    INSERT INTO `t1`(`info`) VALUES('abcd');
    INSERT INTO `t1`(`info`) VALUES('xyz');
    INSERT INTO `t1`(`info`) VALUES('3456789');
    INSERT INTO `t1`(`title`) VALUES('中文');
    INSERT INTO `t1`(`title`) VALUES('中文信息。');

    INSERT INTO `t1`(`id`,`title`) VALUES(10000, 'hello');
    SELECT * FROM t1;

    -- 截断表：drop,create
    -- 保留表结构，删除表里数据，释放了空间，重置自动增长的序列
    TRUNCATE TABLE t1;

    -- 删除表：表结构、数据、空间
    DROP TABLE t1;

    -- 删除数据
    DELETE FROM t1 WHERE XXXX;
    DELETE FROM t1;

    -- GRANT
    CREATE USER 'abc'@'%' identified BY '123456';
    GRANT ALL ON udb.* TO 'abc'@'%';
    GRANT ALL ON mall.* TO 'abc'@'%';
    exit
    -- 使用客户端连接
    mysql -u abc -p
```

启用远程连接

    cd /etc/mysql/mysql.conf.d
    sudo nano mysqld.cnf

    bind-address    =0.0.0.0

    ctrl+O
    ctrl+x

    sudo service mysql restart
    sudo netstat -tpnl | grep mysql

## DML

## TCL

## 任务一
1. 导入 hr.sql 脚本
2. 创建新用户 tom 有访问数据库 hr 的权限
3. 工作台使用 tom 连接

## 任务二
- 学院
- 专业
- 课程
- 教室
- 培养计划
- 学生
- 教师
- 课表
- 成绩
- 选课
- ...
