# STM32F103C8T6 学习笔记

## 芯片概述
- **型号**：STM32F103C8T6
- **内核**：ARM Cortex-M3（属于 ARM v7 架构）
- Cortex 系列分为三个子系列：**A**（应用）、**R**（实时）、**M**（微控制器）

---

## 嵌入式开发基础
- 开发所需资源：
  - 芯片手册
  - 电路原理图
  - SDK / 固件库

---

## 时钟系统

### 系统时钟源（SYSCLK）
可选三种时钟源：
- HSI 振荡器时钟
- HSE 振荡器时钟
- PLL 时钟

### 二级时钟源
- **LSI RC**：40kHz，用于独立看门狗或 RTC 唤醒
- **LSE 晶体**：32.768kHz，用于 RTC

> 每个时钟源可独立启停，以优化功耗。

### 时钟分配
- 外部晶振常见频率：8MHz、12MHz、6MHz
- 通过 PLL 倍频/分频，最高可产生 **72MHz SYSCLK**
- 通过 AHB 总线，再分频为两种外设时钟：
  - **APB1**：最高 36MHz（低速外设：SPI、I2C、UART、USB、看门狗、定时器）
  - **APB2**：最高 72MHz（高速外设：GPIO、SPI1、TIM1/8、UART1）

### 与普通 8/16 位单片机的区别
- 普通单片机：单一系统时钟
- STM32：多时钟频率，内核与外设使用不同时钟，利于低功耗

> **重要**：使用任何外设前，必须先使能其对应的时钟，否则外设无法工作。

---

## 存储器系统
- 程序存储器、数据存储器、寄存器、I/O 端口统一映射到 **4GB 线性地址空间**（32位地址线）
- 所有存储单元都有唯一地址
- 数据存储格式：**小端模式**（最低地址存最低有效字节）

---

## GPIO 控制基础

### GPIO 端口结构
- 每个端口（A、B、C、D...）最多 16 个引脚
- STM32F103C8T6 共 37 个可用引脚：
  - GPA：16
  - GPB：16
  - GPC：3
  - GPD：2

### 每个 GPIO 端口寄存器组
- **2个配置寄存器**：`GPIOx_CRL`（低8位）、`GPIOx_CRH`（高8位）
- **2个数据寄存器**：`GPIOx_IDR`（输入）、`GPIOx_ODR`（输出）
- **1个置位/复位寄存器**：`GPIOx_BSRR`
- **1个复位寄存器**：`GPIOx_BRR`
- **1个锁定寄存器**：`GPIOx_LCKR`

> **注意**：GPIO 寄存器必须按 **32位字** 访问，不支持半字或字节访问。

### GPIO 工作模式
- 输入浮空
- 输入上拉
- 输入下拉
- 模拟输入
- 开漏输出
- 推挽输出
- 推挽复用功能
- 开漏复用功能

---

## 位操作示例

```c
// 将 bit20 置 1
data |= (0x1 << 20);

// 将 bit20 清零
data &= ~(0x1 << 20);

// 将 bit20 取反
data ^= (0x1 << 20);

// 同时清零 bit5, bit8, bit15
data &= ~(0x1 << 5 | 0x1 << 8 | 0x1 << 15);

// 同时置位 bit5, bit8, bit15
data |= (0x1 << 5 | 0x1 << 8 | 0x1 << 15);