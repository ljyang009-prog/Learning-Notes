# Git 版本控制

## Git 命令

仓库：一个文件夹，被初始化后，由git托管

- `git init`
- `git status`
- `git add`
- `git commit`
- `git log`
- `git push` 拉，把远程同步到本地
- `git fetch` 拉，把远程同步到本地
- `git breach` 分支
- `git merge` 合并分支
- `git checkout` 检出，回到过去的某个版本
