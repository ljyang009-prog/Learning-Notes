# 套接字编程  - TCP

## 套接字 socket

套接字是 TCP/IP 实现中出现的概念，封装网络底层通信细节，操作方式类似文件

## IP 地址
接入网络的计算机的逻辑标识，动态分配给计算机的一个标识信息

MAC地址：物理标识，硬件设备的标识，出厂设定，唯一标识

IPv4: 2^32      = 42亿
IPv6: 2^128

## Port 端口号

一台计算机有唯一的 IP 标识，但是同时运行上百的进程，端口号标识收、发数据的进程；端口号也是虚拟，2^16 个，65536个；前1024保留

## TCP 套接字 API
- socket()  创建套接字文件描述符 int
- bind()    绑定地址
- listen()  开启监听，初始化一个队列
- accept()  接收请求
- read/write()  读写
- close()   关闭

## 大小端

- `htons` host to net short
- `htonl` host to net long int
- `ntohs` net to host short
- `ntohl` net to host long

0x12345678

空间：  0   1   2   3
大端： 12  34  56  78

空间：  0   1   2   3
小端： 78  56  34  12

- Little Endian     x86架构
- Big Endian        网络通信，嵌入

## 任务
1. 实现客户端多次读写（循环加退出条件）

2. 实现可以同时连接多个客户端（多线程）