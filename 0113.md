# 结构体数组、指针

## 结构体

## 结构体指针

```c
Stu s[4] = {
        {"lisi", 19},
        {"wangwu", 20},
        {.age = 21, .name = "zhaoliu"}};
    strcpy(s[3].name, "qianqi");
    s[3].age = 22;
    Stu *p = s;
    for (int i = 0; i < 4; i++)
    {
        // printf("%s %d\n", (*p).name, (*p).age);
        printf("%s %d\n", p->name, p->age);
        p++;
    }

```

## 结构体与动态内存管理

## 作业

1. 品牌、产品

品牌

- 名称
- 创建时间
- 产品列表

产品

- 名称
- 价格
- 品牌（）
