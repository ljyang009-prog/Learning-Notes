# pthread 线程

## 线程的生命周期
- 新建  pthread_create()
- 就绪  OS 调度，获得 CPU 的时间片
- 运行  1.结束 2.就绪 3.阻塞
- 阻塞  原因：sleep,wait,让出 CPU 时间片
- 结束

## 分离态与非分离态
- join 非分离态（默认） pthread_join()，若不jion,出现僵死线程
- detach 分离态，子线程与主线程分离，不可join,  后台任务，不需要返回数据，任务结束自动释放资源

### 非分离态

```c
int mian()
{
    // 创建子线程
    pthread_create(t1);    //
    // 其他语句
    code 1
    //pthread_jion(t1);     // 等待子线程 t1 结束，并获得返回值
    code 2
    code 3
    // 结束当前（主）线程，子线程 t1 可以执行
    pthread_exit(NULL);
    return 0;   // 结束进程，所有资源销毁，包括 t1
}





```

## 作业
1. 使用多线程模式统计 Linux 内核中 .c、.h 文件的总行数
    - 每个文件，创建一个线程  ----效率低
    - 线程池 Pool 与任务队列
        - 任务队列，文件的绝对路径，一个任务
        - 线程池里线程，从队列中获得任务，统计


