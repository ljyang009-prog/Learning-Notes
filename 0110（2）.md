# 组合类型

- 数组
- 结构体
- 联合体
- 枚举

## 结构体

```c
    //封装，自定义的数据类型
struct Student {
    //姓名
    char name[32];
    //年龄
    int age;
    //成绩
    float score;
};

    struct Student s1{"Alice", 18, 90.5};
    struct Student s2{"Bob", 19, 85.0};
    struct Student s3{.score = 90.7, .name = "Charlie", .age = 20};
    struct Student s4;
    s4.score = 88.5;
    s4.age = 21;
    //s4.name = "David";  错误
    strcpy(s4.name, "David");
```

## 结构体的大小

内存对齐

- 最大类型长度的整数倍
- 从小到大
- 避免大小交叉存储

## 共用体/联合体 union

内存分布方式不同，内存叠加

```c
union U1
{
    char a;
    short b;
    int c;
};

```

## 枚举

提高了程序的语义性

```c

enum Sex
{
    MALE,
    FEMALE
};

```
