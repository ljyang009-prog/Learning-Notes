# 套接字编程 - UDP

## TCP & UDP

|特征|TCP|UDP|
|--|--|--|
|建立连接|是|否|
|有序|是|否|
|丢失|丢包重发|丢失|
|可靠|是|否|
|数据大小|不受限|65535, MTU=1500|

应用场景：
- TCP:FTP、HTTP、数据可靠有序
- UDP:视频、游戏、传感器

TCP 服务端开发流程
- socket
- setsockopt
- bind
- listen
- accept
- r/w
- close

TCP 客户端开发流程
- socket
- connect -> accecpt
- r/w
- close

## UDP

send 发送
- socket
- sendto(data,addr,...)
- close


recv 接收
- socket
- bind
- recvfrom
- close

## 任务
1. 重写
    - 程序启动指定一个端口号，9000，8000
    - 程序创建俩个子线程：send, recv
        - send
        - recv
        - 共用 fd 还是各自持有一个 fd 