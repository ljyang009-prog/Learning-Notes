# 线程同步

## 互斥锁
## 条件变量

## 信号量
原子性的整数，进行pv操作

参数：
1. 信号量变量
2. 0 进程内，2 进程间
3. 信号量的值：1 二元信号量，n 计数器信号量


sem_init(&sem, 0, 64)
P: 获得资源
    - sem > 0,获得资源，sem--
    - sem = 0,进入阻塞状态
V：释放资源 sem_post()
    - sem++

## 自旋锁 spin_lock

与互斥锁都是排他锁，资源加锁后只能被一个线程独占使用，直到解锁

自旋锁：，为**多核**场景而生，不断轮询 poll 以获得锁，避免了上下文切换时间,CPU占用高
适用临界区比较小的场景；单核场景不适用；自旋锁休眠可能会出现死锁

互斥锁：单核、多核都可以：未获得锁会阻塞，CPU占用低，适用临界区比较大的场景

死锁：多个线程竞争资源时，

哲学家进餐问题


## 读写锁

## 任务

1. 代码统计
    - 线程池
    - 任务队列（循环队列） 队列有任务就让线程执行，直到队列为空
    - 异步与同步
    - 什么 锁
    - 写入文件
2. 电梯调度
    - 两部电梯工作