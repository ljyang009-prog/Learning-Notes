# 指针

## 概念

指针是一种数据类型，存储其他变量的空间地址，

### 数据类型

- 基本类型（原始类型）:int short char float double
- 组合类型（复合类型）：数组、结构体、联合体、枚举、类
- 指针类型
- void类型

## 定义

类型 *变量名

```c
    //基本类型
    int    a;      //4
    char   c;      //1
    double g;      //8
    
    //指针变量，空间大小与平台有关 32位系统4字节， 64位系统8字节
    int    *p1;  //8
    char   *p2;  //8
    double *p3;  //8

    p1 = &a;    //a的地址存入变量p1,指针p1指向变量a
    int x;
    p1 = &x;    //x的地址存入变量p1,指针p1指向变量x
```

## 操作

### 赋值

```c
    int a = 10;
    int *p;
    p = &a;  //p指向a
    *p = 20; //p指向的变量a的值改为20
```

### 指针运算

```c

    //* 声明指针变量
    //& 取地址运算符
    int a = 10;
    int *p;
    p = &a;  //p指向a
    p++;     //p指向a的下一个地址
    *p = 20; //p指向的变量a的值改为20
    char *p4 = &a;//(x)指针的类型不能改变
    //声明，未赋值的指针叫野指针
    int *p5;
    p5 = &a;//(x)野指针不能解引用

```

rust

### * 运算符

- 若在数据类型的后面，*声明指针类型的变量
- 若在变量名前面，*表示取出指针指向的变量的值(解引用)
- 解引用:访问指针指向的空间，可以间接修改指针指向的变量

```c
    int a = 10;
    int *p;
    p = &a;  //p指向a
    *p = 20; //p指向的变量a的值改为20
```

## 应用

- 函数参数传递
- 减小内存浪费
- 数组遍历 （指针运算 +，-）
