# 数据库

## 回顾
- SQL: DDL、DML、TCL
- 数据对象:表、索引、视图、过程、函数、触发器、约束
- DDL（数据定义语言）:CREATE,ALTER,DROP,GRANT
- DML（数据操作语言）:INSERT,SELECT,UPDATE,DELETE
- 表连接
    - 内连接：交集
    - 外连接
        - 左外连接：
        - 右外连接：
        - 全外连接：并集
```sql
操作
    - 交集

select t1.val from t1,t2 where t1.val = t2.val;
select t1.* from t1 inner join t2 on t1.id = t2.id;

    - 并集 UNION

SELECT id,val
FROM t1
UNION ALL
SELECT id,val
FROM t2;

    - 差集 
select t1.* from t1 left join t2 on t1.id = t2.id where t2.id is null;
select t2.* from t2 left join t1 on t2.id = t1.id where t1.id is null;



--- 查询语句语法结构
SELECT
    c1,c2 
FROM
    t1
JOIN
    t2
ON
    t1 = t2
WHERE 过滤条件
GROUP BY 分组/聚合函数
HAVING 过滤分组的数据
ORDER BY 排序
LIMIT 分页
```

## 事务处理

事务：一个业务操作包含多个数据操作DML，这多个数据操作在逻辑上是一个整体，它的执行或撤回称为一个事务：包含四个特性：原子性、一致性、隔离性、持久性

ACID
- 原子性：构成事物的多个数据操作是一个整体，不可分割：执行要么成功要么回滚
- 一致性：事务执行前后，数据一致
- 隔离性：多个事务执行时，相互隔离，隔离级别：读未提交，读已提交，可重复读，串行化
- 持久性：事务结束，数据写入磁盘

TCL
- `BEFIN`       开启手动事务模式，直到提交
- `COMMIT`      提交
- `ROLLBACK`    回滚
### 隔离级别

|隔离级别|`脏读`|`不可重复读`|`幻读`|
|--|--|--|--|
|读未提交| + | + | + |
|读已提交| - | + | + |
|可重复读| - | - | + |
|串行化| - | - | - |

脏读：读取其他事务未提交的数据
不可脏读：事务中，多次读取的数据不一致
幻读:事务中，因插入和删除引发的数据不一致性



```sql
-- 转账
BEGIN;

update account set balance = balance - 5 where id = 1;
update account set balance = balance + 10000 where id = 4;
insert into log(`from`,`to`,`amount`) values(1,2,5);
```

## 索引

目标：提高查询速度，负面：插入、删除、更新

具体实现：

自动创建索引：主键列、外键列、唯一约束
手动创建索引

## 视图