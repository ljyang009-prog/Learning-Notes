# 字符串

## 定义

```c
    char s1[16] = "abc";   //sizeof = 16 strlen = 3

```

## 函数

在标准库 string.h 中，提供了许多处理字符串的函数，例如：

- `strlen`：计算字符串长度
- `strcpy`：字符串复制
- `strcat`：字符串连接
- `strcmp`：字符串比较
- `strchr`：查找字符
- `strstr`：查找子串
- `strnxxx` n范围安全的，多一个参数
- `memcpy`：内存复制
- `memmove`：内存移动
- `memcmp`：内存比较
- `memset`：内存设置

```c
// {} 数据对象，结构体，键值对，字典
// [] 数组，列表，集合

```

### 终端读取字符串

- scanf()
- gets(s)
- fgets(s, sizeof(s), stdin)

## 应用

1.定义一个函数，传入整数转16进制

char x[] = "0123456789abcdef";
31    0x1F
1     15
0     1

2.把1345转汉字大写形式

3.大数值的加减乘除
    char a[32];
    char b[32];
    a+b
